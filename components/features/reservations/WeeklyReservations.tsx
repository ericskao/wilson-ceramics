'use client';

import { useState } from 'react';

const WeeklyReservations = () => {
  const [weekOffset, setWeekOffset] = useState(0);
  return <div>Weekly Reservations</div>;
};
export default WeeklyReservations;
